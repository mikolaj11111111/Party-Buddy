# Sources for Hangman word dataset

Dataset files:

- `words.json`

The Hangman word batch was locally authored in Polish for Party Buddy.
Words, hints, categories, and difficulty labels are original local text.

The dataset is a static word bank, not a SQL database. It can later be moved to
SQLite together with admin UI if the project needs runtime editing.
